import { ShowOrHideFilterDirective } from './show-or-hide-filter.directive';

describe('ShowOrHideFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOrHideFilterDirective();
    expect(directive).toBeTruthy();
  });
});
